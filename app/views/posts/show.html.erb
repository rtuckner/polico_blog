<p id="notice"><%= notice %></p>

<div class="wrapper">
  <div class="left-side">
    <p>
      <strong>Title:</strong>
      <%= @post.title %>
    </p>

    <p class="cool">
      <strong>Body:</strong>
      <%= @post.body %>
    </p>
  </div>
  <div class="right-side">
  </div>
</div>
<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
