<p id="notice"><%= notice %></p>

<div class="wrapper">
  <div class="left-side">
    <h2>
      <strong>Title:</strong>
      <%= @post.title %>
    </h2>
      <p class="cool">
        <strong></strong>
        <%= @post.body %>
      </p>
  </div>
  <div class="right-side">
  </div>
</div>
<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
